<!DOCTYPE html>
<html lang='en'>
<head>
  <% include partials/_header %>
</head>
<body>
  <main>
    <h2>url Database</h2>
      <% Object.keys(urls).forEach(function (key) { %>
        <li> <%= key %>: <%= urls[key] %> <form method='POST' action='/urls/<%= key %>/delete'><input type='submit', value='Delete'></form></li>
      <% }); %>
  </main>
</body>
</html>